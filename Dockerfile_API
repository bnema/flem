FROM node:lts-alpine

WORKDIR /app

COPY . .

cd /app/api

RUN npm install

RUN npm run build

# Define some ENV Vars
ENV PORT=3333 \
  DIRECTORY=/app \
  IS_DOCKER=true


CMD ["node", "dist/index.js"]

EXPOSE ${PORT}:80