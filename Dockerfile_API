FROM node:20
WORKDIR /app
EXPOSE 3333
# Copy the api.js file to the container at /api.js
RUN npm install cors

COPY ["dist/api.js", "./"]

# Run the api.js when the container launches
CMD ["node", "api.js"]
